interface:
  display_name: "BiliTickerSkill"
  short_description: "通过 skill 驱动本地 biliTickerBuy 抢票流程。"
  default_prompt: "使用 $BiliTickerSkill 定位本地 biliTickerBuy 项目，检查环境和登录状态，引导用户选择日期与票档，并准备或执行正确的抢票流程。"

policy:
  allow_implicit_invocation: false
